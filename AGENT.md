# 🤖 AGENT: Инструкции для AI-агента Antigravity (Epistemic OS v2.0)

Этот файл содержит обязательные правила поведения агента в рабочем пространстве **epios (Epistemic OS)**. Он объединяет принципы Epistemic Clarity с инженерными стандартами платформы Antigravity.

---

## 🏗️ 1. Ядро Epistemic OS: Принципы и Формула

**Epistemic OS — это операционный слой для структурированного мышления и принятия решений.**
Агент обязан соблюдать принципы, описанные в `docs/01_foundation/EPIOS-01-architecture-foundation.md`:
1. **Стратегическая формула:** `situation → distinction → evidence → artifact → decision → action`. Все действия должны быть прослеживаемыми (traceable).
2. **Epistemic Clarity:** Четкое различие между утверждениями (claims), доказательствами (evidence) и артефактами (artifacts).
3. **Human Sovereignty:** Все значимые действия требуют явного подтверждения человеком (human-in-the-loop).
4. **Contract-first & Domain-driven:** Вся логика сосредоточена в `packages/domain`. Зависимости направлены внутрь (`domain ← ports ← application`). Инфраструктура зависит от портов.

## 📋 2. Погружение в контекст (В начале диалога)

**В начале КАЖДОГО нового чата агент ОБЯЗАН:**
1. Прочитать Оперативный план: `STATUS.md` (здесь описаны текущие спринты и задачи).
2. Ознакомиться с Проектным планом: `docs/04_delivery/V1_1_PROJECT_PLAN_ADR_REVIEW.md` (для понимания глобального контекста).
3. Прочитать карту архитектуры и процесса: `docs/01_foundation/EPIOS-04-engineering-process.md`.
4. Ознакомиться с `PROJECT_MAP.md` (если отсутствует — создать на базе структуры папок).
5. Кратко сообщить пользователю: текущий статус MVP (какой этап, что будем делать).
6. **Не требовать от пользователя повторного объяснения контекста** — агент восстанавливает его самостоятельно через чтение файлов.

## ⚠️ 3. Правило планирования (ОБЯЗАТЕЛЬНО)

**Прежде чем писать код для новых функций или рефакторинга, агент ОБЯЗАН:**
1. Составить краткий технический план предлагаемых изменений (в соответствии с шаблоном PR из `.github/PULL_REQUEST_TEMPLATE.md`).
2. Сообщить этот план пользователю.
3. **Дождаться явного утверждения** плана.
4. Приступать к выполнению только после получения подтверждения.

## 🔄 4. Итеративный процесс разработки (Спринты)

**Агент ОБЯЗАН придерживаться строгого цикла:**
1. **Дробление**: Реализовывать MVP короткими вертикальными срезами (vertical slices). Один PR = одна фича/архитектурный слой.
2. **Завершение спринта**: Каждая итерация должна заканчиваться цепочкой действий:
    - **Обновление STATUS.md**: Все задачи спринта отмечены `[x]`.
    - **Обновление карты**: Актуализировать `PROJECT_MAP.md` при изменении структуры (`pnpm refresh-map`).
    - **Sprint QA Gate (ОБЯЗАТЕЛЬНО)**: Запустить `pnpm sprint:qa -- --sprint <N>`. Спринт НЕ может быть закрыт без статуса `PASS`. Регламент: `docs/04_delivery/SPRINT_QA_REGULATION.md`.
    - **Аудит документации**: Выполнить аудит документации `docs/` на соответствие новому коду (устранить дублирования, обновить Roadmaps) согласно регламенту `docs/00_project/GOVERNANCE_PLAN.md`.
    - **commit**: Фиксация изменений с семантическим префиксом (`feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `test:`, `security:`).
    - **push**: Отправка кода в удаленный репозиторий.

## ✅ 5. Работа со Спринтами и Оперативным планом (STATUS.md)

**Агент ОБЯЗАН соблюдать следующий процесс работы со спринтами:**
1. **Поиск задачи:** Агент находит в `STATUS.md` спринт со статусом `🏃 В работе` и берет задачи из его чек-листа.
2. **Изучение контекста (КРИТИЧНО):** Чек-лист в `STATUS.md` содержит только краткие тезисы. Перед реализацией агент ОБЯЗАН перейти по ссылкам на технический контекст (указаны под заголовком спринта, например, ссылки на `docs/04_delivery/` или файлы в `docs/03_specs/`) для получения полного ТЗ и контрактов.
3. **Учет рисков:** Если спринт помечен как `[🔒 УНИКАЛЬНЫЙ]`, агент должен понимать, что задача затрагивает критические узлы, и действовать крайне осторожно. Для `[НЕЗАВИСИМЫХ]` спринтов допустима параллельная работа.
4. **Обновление чек-листа:** После завершения задачи агент ОБЯЗАН отметить пункт крестиком `[x]` в `STATUS.md`.
5. **Подведение итогов:** Записать краткий итог этапа в коммит или лог: что сделано, какие контракты затронуты.
6. **Не переходить к следующей задаче** без обновления чек-листа в Оперативном плане.

## 🧪 6. Обязательное самотестирование

**Перед тем как сообщить пользователю о готовности, агент ОБЯЗАН:**
1. Запустить тесты в соответствующих пакетах (`pnpm test`).
2. Для UI — проверить результат через `browser_subagent`.
3. Для API — проверить работоспособность через интеграционные тесты или curl.
4. Убедиться, что нет новых ошибок в консоли и старый функционал не сломан.
5. **При завершении спринта** — обязательно выполнить `pnpm sprint:qa -- --sprint <N>` и дождаться статуса PASS.
6. **commit & push**: Фиксация проверенного результата.
7. Только после этого сообщить: «Этап завершён и проверен».

## 🚫 7. Ограничения и запреты

1. **Технологический стек:** Основной стек — **TypeScript / Node.js / pnpm**. Использование Python запрещено (кроме ML-сервисов с ADR).
2. **Чистота архитектуры:** Категорический запрет на импорт из `infrastructure` в `domain` или `application`.
3. **Запрет на старые концепции:** Не использовать подходы ChatAVG v2.3, если они противоречат EPIOS (например, неструктурированные workflow без Epistemic Nodes).
4. **Следование ADR:** Все архитектурные решения фиксируются в `docs/01_foundation/` или `docs/02_adrs/`.
5. **Обновление KI:** После изменения AGENT.md — обновить KI-артефакт `agent_rules.md`.

---
*Агент должен проверять наличие файла AGENT.md и следовать всем инструкциям при каждом новом запросе.*
